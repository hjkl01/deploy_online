"use client";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";const API=process.env.NEXT_PUBLIC_API_URL||"";const emptyProject={name:"",description:"",branch:"main",shell:"bash",enabled:true,steps:[],environment:[]};const emptyStep={name:"",step_type:"command",cwd:"~",command:"",enabled:true,timeout:3600,continue_on_error:false};export default function Edit(){const r=useRouter(),[id,setId]=useState<string|null>(null),[p,setP]=useState<any>(null),[allowed,setAllowed]=useState(true);useEffect(()=>{const x=new URLSearchParams(window.location.search).get("id");setId(x);if(!x)setP(emptyProject)},[]);useEffect(()=>{if(!id)return;fetch(API+"/api/projects/"+id,{credentials:"include"}).then(x=>{if(x.status===403){setAllowed(false);return null}return x.json()}).then(setP)},[id]);if(!allowed)return <main className="wrap"><div className="card"><h1>无权限</h1><a href="/projects">返回项目</a></div></main>;if(p===null)return <main className="wrap">加载中...</main>;const save=async()=>{const url=id?API+"/api/projects/"+id:API+"/api/projects",method=id?"PUT":"POST",body={...p,steps:p.steps.map((s:any)=>{const{position,id,...v}=s;return v}),environment:p.environment.map((e:any)=>{const{id,...v}=e;return v})};const x=await fetch(url,{method,headers:{"Content-Type":"application/json"},credentials:"include",body:JSON.stringify(body)});if(!x.ok){alert((await x.json()).detail||"保存失败");return}r.push("/projects")};const remove=async()=>{if(!id||!confirm("确定删除这个项目吗？"))return;const x=await fetch(API+"/api/projects/"+id,{method:"DELETE",credentials:"include"});if(x.ok)r.push("/projects");else alert((await x.json()).detail||"删除失败")};return <main className="wrap"><div className="topbar"><h1>{id?"编辑":"添加"}项目</h1><a href="/projects">项目列表</a></div><div className="card"><label>项目名称</label><input value={p.name} onChange={e=>setP({...p,name:e.target.value})}/><label>说明</label><input value={p.description} onChange={e=>setP({...p,description:e.target.value})}/><label>Git 分支</label><input value={p.branch} onChange={e=>setP({...p,branch:e.target.value})}/><label>Shell</label><select value={p.shell} onChange={e=>setP({...p,shell:e.target.value})}><option value="bash">bash</option><option value="zsh">zsh</option></select><label><input className="check" type="checkbox" checked={p.enabled} onChange={e=>setP({...p,enabled:e.target.checked})}/> 启用项目</label></div><h2>部署步骤</h2>{p.steps.map((s:any,i:number)=><div className="card" key={i}><div className="row"><h3>Step {i+1}</h3><button className="danger" onClick={()=>setP({...p,steps:p.steps.filter((_:any,j:number)=>j!==i)})}>删除</button></div><label>名称</label><input value={s.name} onChange={e=>{s.name=e.target.value;setP({...p})}}/><label>类型</label><select value={s.step_type} onChange={e=>{s.step_type=e.target.value;setP({...p})}}><option value="command">command</option><option value="git_pull">git pull</option></select><label>cwd（必须从 ~/ 开始）</label><input value={s.cwd} onChange={e=>{s.cwd=e.target.value;setP({...p})}}/><label>命令</label><textarea value={s.command} disabled={s.step_type==="git_pull"} onChange={e=>{s.command=e.target.value;setP({...p})}}/><label>超时秒数</label><input type="number" value={s.timeout} onChange={e=>{s.timeout=Number(e.target.value);setP({...p})}}/><label><input className="check" type="checkbox" checked={s.enabled} onChange={e=>{s.enabled=e.target.checked;setP({...p})}}/> 启用</label><label><input className="check" type="checkbox" checked={s.continue_on_error} onChange={e=>{s.continue_on_error=e.target.checked;setP({...p})}}/> 失败后继续</label></div>)}<button onClick={()=>setP({...p,steps:[...p.steps,{...emptyStep}]})}>+ 添加 Step</button><h2>环境变量</h2>{p.environment.map((e:any,i:number)=><div className="card" key={"env"+i}><div className="row"><input placeholder="KEY" value={e.key} onChange={x=>{e.key=x.target.value;setP({...p})}}/><input placeholder={e.is_secret?"留空则保留原密码":"VALUE"} type={e.is_secret?"password":"text"} value={e.value} onChange={x=>{e.value=x.target.value;setP({...p})}}/><label><input className="check" type="checkbox" checked={e.is_secret} onChange={x=>{e.is_secret=x.target.checked;e.value="";setP({...p})}}/> 敏感</label><button className="danger" onClick={()=>setP({...p,environment:p.environment.filter((_:any,j:number)=>j!==i)})}>删除</button></div></div>)}<button onClick={()=>setP({...p,environment:[...p.environment,{key:"",value:"",is_secret:false}]})}>+ 添加环境变量</button><div className="actions"><button onClick={save}>保存</button>{id&&<button className="danger" onClick={remove}>删除项目</button>}</div></main>}