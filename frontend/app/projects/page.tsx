"use client";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";const API=process.env.NEXT_PUBLIC_API_URL||"";export default function Projects(){const[p,setP]=useState<any[]>([]);const r=useRouter();useEffect(()=>{fetch(API+"/api/projects",{credentials:"include"}).then(x=>x.json()).then(setP)},[]);return <main className="wrap"><h1>项目</h1>{p.map(x=><div className="card" key={x.id}><h2>{x.name}</h2><p>{x.description}</p><p>{x.shell} · {x.branch}</p><button onClick={()=>r.push("/projects/edit?id="+x.id)}>编辑</button></div>)}</main>}