"use client";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";const API=process.env.NEXT_PUBLIC_API_URL||"";export default function Home(){const[p,setP]=useState<any[]>([]),[me,setMe]=useState<any>(null),[login,setLogin]=useState(false);const r=useRouter();const load=()=>Promise.all([fetch(API+"/api/projects",{credentials:"include"}),fetch(API+"/api/auth/me",{credentials:"include"})]).then(async([a,b])=>{if(a.status===401){setLogin(true);return}setP(await a.json());if(b.ok)setMe(await b.json())}).catch(()=>{});useEffect(()=>{load()},[]);if(login)return <Login onLogin={()=>{setLogin(false);load()}}/>;return <main className="wrap"><div className="topbar"><div><h1>deploy_online</h1>{me&&<p>当前用户：{me.username} · {roleName(me.role)}</p>}</div><div>{me?.role==="admin"&&<a href="/users">用户管理</a>} <a href="/projects">项目管理</a> <a href="/deployments">部署记录</a> <button className="secondary" onClick={async()=>{await fetch(API+"/api/auth/logout",{method:"POST",credentials:"include"});setLogin(true)}}>退出</button></div></div><div className="grid">{p.map(x=><Project key={x.id} p={x} router={r} canDeploy={me?.role==="admin"||me?.role==="operator"}/>)}</div></main>}function roleName(x:string){return x==="admin"?"管理员":x==="operator"?"操作员":"查看者"}function Login({onLogin}:{onLogin:()=>void}){const[u,su]=useState("admin"),[pw,sp]=useState(""),[err,se]=useState("");return <main className="wrap"><div className="card"><h1>登录</h1><input placeholder="用户名" value={u} onChange={e=>su(e.target.value)}/><input placeholder="密码" type="password" value={pw} onChange={e=>sp(e.target.value)}/><button onClick={async()=>{const r=await fetch(API+"/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},credentials:"include",body:JSON.stringify({username:u,password:pw})});if(r.ok)onLogin();else se("用户名或密码错误")}} >登录</button>{err&&<p className="error">{err}</p>}</div></main>}function Project({p,router,canDeploy}:{p:any,router:any,canDeploy:boolean}){return <div className="card"><h2>{p.name}</h2><p>{p.description}</p><p>{p.shell} · {p.branch}</p>{canDeploy&&<button disabled={!p.enabled} onClick={async()=>{const x=await fetch(API+"/api/projects/"+p.id+"/deploy",{method:"POST",credentials:"include"});if(!x.ok){alert((await x.json()).detail);return}const d=await x.json();router.push("/deployments?id="+d.id)}}>立即部署</button>}</div>}